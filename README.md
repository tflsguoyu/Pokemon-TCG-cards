# Pokemon TCG Cards

A static Pokemon TCG national dex gallery for GitHub Pages.

The page loads English card data from TCGdex, supplements set codes from Pokemon TCG API, and caches results in the browser for faster repeat visits.

Open `index.html` locally or publish the repository with GitHub Pages from the `main` branch root.
